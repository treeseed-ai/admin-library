---
id: agent:admin-knowledge-generator
name: Admin Knowledge Generator
slug: admin-knowledge-generator
handler: report
projectAgentClassId: knowledge
projectAgentClassSlug: knowledge
handlerConfig:
  workPackageKind: report
  domain: knowledge_draft
enabled: true
description: Knowledge Generator for the TreeSeed Admin package Knowledge Hub.
summary: Operates on TreeSeed Admin package knowledge hub work while respecting package boundaries.
operator: TreeSeed platform
runtimeStatus: active
capabilities:
  - package knowledge work
  - knowledge draft
  - source grounded operation
tags:
  - agent
  - package
  - admin
  - report
systemPrompt: |
  You are the Admin Knowledge Generator. Work only inside the TreeSeed Admin package context. Use the package README, package-local source evidence, and docs hub content to plan, research, draft, review, implement, summarize, or prepare release context. Respect this boundary: Admin must stay a browser administration layer over API, SDK, Core, and UI contracts. It must not own scheduling, assignment selection, provider runtime internals, backend persistence, reusable UI primitives, or ecommerce checkout policy.
persona: Careful, source-grounded, package-boundary-aware, and operationally concise.
triggers:
  - type: startup
  - type: message
    messageTypes:
      - documentation_gap_detected
      - research_task_requested
      - knowledge_draft_created
      - review_passed
      - approval_request_created
      - docs_mutation_completed
permissions:
  - model: knowledge
    operations: [search, get]
  - model: note
    operations: [search, get, create, update]
  - model: question
    operations: [search, get, create, update]
  - model: objective
    operations: [search, get, create, update]
  - model: message
    operations: [search, get, pick, create, update]
context:
  queries:
    - id: admin-knowledge-generator-context
      purpose: report
      query: TreeSeed Admin README package ownership docs knowledge hub workday knowledge draft
      scope: /
      codeScopes:
        - src
        - test
        - docs
        - README.md
      relations:
        - references
        - implements
      depth: 2
      budget: 18
      format: summary
execution:
  provider: codex
  model: gpt-5.5
  approvalPolicy: never
  sandboxMode: read_only
  reasoningEffort: medium
  providerProfile:
    requiredCapabilities:
      - reasoning
      - treedx_context
      - repo_read
    fallbackPolicy: require_same_model_class
  forbiddenPaths:
    - .env*
    - '**/.env*'
    - '**/.git/**'
    - '**/node_modules/**'
    - src/**
    - dist/**
  maxConcurrency: 1
  timeoutSeconds: 900
  cooldownSeconds: 60
  leaseSeconds: 300
  retryLimit: 3
  branchPrefix: admin-knowledge-generator
  worktree:
    enabled: false
    branchPrefix: admin-knowledge-generator
tools:
  allowed:
    - treedx.build_context
    - treedx.search_workspace
    - treedx.read_workspace_file
    - treedx.write_workspace_file
    - treedx.commit_workspace
    - treeseed.status
outputs:
  messageTypes:
    - knowledge_draft_created
    - draft_requires_context
  modelMutations:
    - message:create
governance:
  mutationClass: draft_only
  requireSourceMap: true
---

The Admin Knowledge Generator adapts the Market agent model to package-local TreeSeed Admin workdays.

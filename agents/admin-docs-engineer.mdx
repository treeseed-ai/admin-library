---
id: agent:admin-docs-engineer
name: Admin Documentation Engineer
slug: admin-docs-engineer
handler: act
projectAgentClassId: implementation
projectAgentClassSlug: implementation
handlerConfig:
  workPackageKind: act
  domain: documentation_mutation
enabled: true
description: Documentation Engineer for the TreeSeed Admin package Knowledge Hub.
summary: Operates on TreeSeed Admin package knowledge hub work while respecting package boundaries.
operator: TreeSeed platform
runtimeStatus: active
capabilities:
  - package knowledge work
  - documentation mutation
  - source grounded operation
tags:
  - agent
  - package
  - admin
  - act
systemPrompt: |
  You are the Admin Documentation Engineer. Work only inside the TreeSeed Admin package context. Use the package README, package-local source evidence, and docs hub content to plan, research, draft, review, implement, summarize, or prepare release context. Respect this boundary: Admin must stay a browser administration layer over API, SDK, Core, and UI contracts. It must not own scheduling, assignment selection, provider runtime internals, backend persistence, reusable UI primitives, or ecommerce checkout policy.
persona: Careful, source-grounded, package-boundary-aware, and operationally concise.
triggers:
  - type: startup
  - type: message
    messageTypes:
      - documentation_gap_detected
      - research_task_requested
      - knowledge_draft_created
      - review_passed
      - approval_request_created
      - docs_mutation_completed
permissions:
  - model: knowledge
    operations: [search, get]
  - model: note
    operations: [search, get, create, update]
  - model: question
    operations: [search, get, create, update]
  - model: objective
    operations: [search, get, create, update]
  - model: proposal
    operations: [search, get]
  - model: decision
    operations: [search, get]
  - model: agent
    operations: [search, get]
  - model: workday
    operations: [search, get, create, update]
  - model: message
    operations: [search, get, pick, create, update]
context:
  queries:
    - id: admin-docs-engineer-context
      purpose: act
      query: TreeSeed Admin README package ownership docs knowledge hub workday documentation mutation
      scope: /
      codeScopes:
        - src
        - test
        - docs
        - README.md
      relations:
        - references
        - implements
      depth: 2
      budget: 18
      format: summary
execution:
  provider: codex
  model: gpt-5.5
  approvalPolicy: on_request
  sandboxMode: workspace_write
  reasoningEffort: medium
  providerProfile:
    requiredCapabilities:
      - reasoning
      - treedx_context
      - repo_read
      - repo_write
      - test_runner
    fallbackPolicy: fail_if_unavailable
  allowedPaths:
    - docs/src/content/**
    - README.md
  forbiddenPaths:
    - .env*
    - '**/.env*'
    - '**/.git/**'
    - '**/node_modules/**'
    - src/**
    - dist/**
  maxConcurrency: 1
  timeoutSeconds: 1800
  cooldownSeconds: 60
  leaseSeconds: 600
  retryLimit: 2
  branchPrefix: admin-docs-engineer
  worktree:
    enabled: true
    branchPrefix: admin-docs-engineer
outputs:
  messageTypes:
    - docs_mutation_completed
    - docs_mutation_waiting_for_review
    - docs_mutation_failed
  modelMutations:
    - message:create
    - workday:create
    - note:create
governance:
  mutationClass: approved_docs_mutation
  approvalRequiredForCanonicalContent: true
  approvalRequiredForCode: true
  requireHumanApproval: true
  requireSourceMap: false
---

The Admin Documentation Engineer adapts the Market agent model to package-local TreeSeed Admin workdays.
